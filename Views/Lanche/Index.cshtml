@model List<TotemPWA.Models.Product>

@{
    ViewBag.Title = "Ingredientes";
    Layout = "~/Views/Shared/_WelcomeLayout.cshtml";
}

<link rel="stylesheet" href="~/css/Editar.css">

<div class="content">
    <div class="container mt-5">
        <h1>Ingredientes Disponíveis</h1>

        <button onclick="location.href = `/Lanche/Create`;">Adicionar Lanche</button>

        @foreach (var ing in Model)
        {
            <div class="contentIngredient ingrediente-@ing.Id">
                <img class="fotoIngrediente" src="img/@ing.Foto" alt="@ing.Name" />
                <h2>@ing.Name</h2>

                <form asp-action="Delete" method="post" >
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" value="@ing.Id" />
                    <button type="submit" class="btn btn-danger">Excluir</button>
                </form>

                <button class="btn btn-primary" onclick="location.href = `/Lanche/Editar?id=@ing.Id`;">Editar</button>
            </div>
        }
    </div>
</div>
